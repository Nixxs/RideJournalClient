import Box from "@mui/material/Box";


function VehicleTimeline({vehicleEvents}) {
    return (
      <Box sx={{ background: "red"}}>
        <p>this is the timeline</p>
      </Box>
    );
};

export default VehicleTimeline;

